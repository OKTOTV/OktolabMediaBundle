{% if player_type == 'jwplayer' %}
    {
        {% if origin.logo is not defined or origin.logo is empty or origin.logo is null %}
            "file": "{{ absolute_url(asset('bundles/oktolabmedia/img/logo.png')) }}",
        {% else %}
            "file": "{{ absolute_url(asset(origin.logo)) }}",
        {% endif %}
            "link": "{{ origin.url }}",
            "margin": "{{ origin.margin }}",
            "position": "{{ origin.position }}"
    }
{% endif %}
