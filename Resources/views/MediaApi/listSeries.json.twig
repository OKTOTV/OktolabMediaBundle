{% spaceless %}
{% set groups = ['oktolab']|merge([serialization_group]) %}
{
    "seriess": {{ seriess.items | serialize('json', serialization_context().setGroups(groups)) }},
    "navigation": {{ knp_pagination_render(seriess, "OktolabMediaBundle:MediaApi:jsonPagination.html.twig")}}
}
{% endspaceless %}
